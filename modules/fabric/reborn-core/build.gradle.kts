metadata {
    waila("lol.bai.megane.module.reborncore.MeganeRebornCore")

    fmj {
        depends(
            "reborncore" to "*"
        )
    }
}

dependencies {
    modImplementation(deps.fabric.rebornCore)
}
